export const money=n=>`$${Number(n||0).toFixed(4)}`; export const compact=n=>Intl.NumberFormat('en',{notation:'compact',maximumFractionDigits:1}).format(Number(n||0)); export const ms=n=>`${Number(n||0).toFixed(0)} ms`; export const shortId=x=>x?`${x.slice(0,8)}…${x.slice(-4)}`:'—'; export const date=x=>x?new Date(x).toLocaleString([], {month:'short',day:'numeric',hour:'2-digit',minute:'2-digit'}):'—';
