import {Cloud,CheckCircle2} from 'lucide-react';import {api} from '../api/client';import {useApi} from '../hooks/useApi';import Panel from '../components/ui/Panel';import Badge from '../components/ui/Badge';
export default function Providers(){const q=useApi(api.providers,[]);return <div><div className="page-heading"><div><div className="eyebrow">INTEGRATIONS</div><h1>Providers</h1><p>Registered provider adapters available to the routing engine.</p></div></div><div className="provider-grid">{(q.data||[]).map(p=><Panel key={p.id} title={p.name}><div className="provider-body"><div className="provider-icon"><Cloud size={21}/></div><div><div className="provider-slug mono">{p.slug}</div><Badge tone="success"><CheckCircle2 size={12}/> Registered</Badge></div></div></Panel>)}</div></div>}
