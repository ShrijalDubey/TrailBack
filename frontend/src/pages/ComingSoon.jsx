import {useLocation} from 'react-router-dom';import {Beaker,Database,LineChart,Timer} from 'lucide-react';import Panel from '../components/ui/Panel';
const meta={cache:{title:'Semantic cache',desc:'Cache controls, hit-rate analytics and cache entry management are planned for a future backend release.',icon:Database},benchmarks:{title:'Benchmarks',desc:'Run model comparisons and persist benchmark results once the benchmark API is available.',icon:Timer},evaluations:{title:'Evaluations',desc:'Evaluation datasets, runs and quality scoring will land here when the evaluation API is implemented.' ,icon:Beaker}};
export default function ComingSoon(){const key=useLocation().pathname.slice(1);const m=meta[key]||meta.cache;const I=m.icon;return <div className="soon-page"><div className="soon-symbol"><I size={30}/></div><div className="eyebrow">ROADMAP</div><h1>{m.title}</h1><p>{m.desc}</p><span className="badge">COMING SOON</span><Panel title="Why this is not mocked" subtitle="TrailBack console policy"><div className="explain"><LineChart size={18}/><span>The frontend only visualizes capabilities currently backed by real FastAPI endpoints. Until this feature exists server-side, the dashboard remains intentionally read-only.</span></div></Panel></div>}
