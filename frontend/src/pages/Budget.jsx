import {useState} from 'react';import {BellRing,SlidersHorizontal} from 'lucide-react';import Panel from '../components/ui/Panel';
export default function Budget(){const [limit,setLimit]=useState(25);return <div><div className="page-heading"><div><div className="eyebrow">FINOPS</div><h1>Budget & alerts</h1><p>The data model for budgets and alert rules is not exposed by the current backend.</p></div></div><div className="coming-feature"><div className="coming-icon"><BellRing/></div><div><BadgeLike text="PLANNED"/><h2>Budget controls are coming soon</h2><p>The backend currently exposes request cost analytics, but no budget or alert CRUD endpoints. This screen intentionally avoids fake data and shows the planned control surface instead.</p></div><div className="slider-preview"><label>Planned monthly limit <b>${limit}</b></label><input type="range" min="5" max="100" value={limit} onChange={e=>setLimit(e.target.value)}/><small>Preview only — not persisted.</small></div></div></div>}
function BadgeLike({text}){return <span className="badge">{text}</span>}
