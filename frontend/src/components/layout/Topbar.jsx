import {Bell,Command,Search} from 'lucide-react';import {useAuth} from '../../context/AuthContext';
export default function Topbar(){const {session}=useAuth();return <header className="topbar"><div className="crumb"><span>Workspace</span><b>/</b><strong>{session?.project_name||'Project'}</strong></div><div className="top-actions"><div className="search"><Search size={16}/><input placeholder="Search requests, models…"/><kbd>⌘ K</kbd></div><button className="icon-btn"><Bell size={18}/><i/></button><div className="avatar">{(session?.project_name||'T').slice(0,1).toUpperCase()}</div></div></header>}
